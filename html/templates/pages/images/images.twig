{% if images is empty %}
  {% include 'errors/generic.twig' with { message: 'There are no images available. Please click <a href="index.php?page=list_days">here</a> to return to the image overview page' } %}
{% else %}
  <div id="gallery" class="row g-2">
    {% for image in images %}
      <div class="col-1" data-src="{{ image.full }}">
        <a href="#">
          <img src="{{ image.thumbnail }}" loading="lazy" class="img-fluid rounded">
        </a>
      </div>
    {% endfor %}
  </div>


  <link href="https://cdn.jsdelivr.net/npm/lightgallery/css/lightgallery-bundle.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/lightgallery/lightgallery.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/lightgallery/plugins/zoom/lg-zoom.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/lightgallery/plugins/thumbnail/lg-thumbnail.min.js"></script>

  <script>
    lightGallery(document.getElementById('gallery'), {
      selector: 'div',
      plugins: [lgZoom, lgThumbnail],
      speed: 300,
      download: false,
      thumbnail: true,
      zoom: true
    });
  </script>
{% endif %}